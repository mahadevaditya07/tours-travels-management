const bcrypt=require('bcryptjs'),jwt=require('jsonwebtoken'),User=require('../models/User');const token=id=>jwt.sign({id},process.env.JWT_SECRET,{expiresIn:process.env.JWT_EXPIRES_IN||'7d'});exports.register=async(req,res)=>{try{const{name,email,password,phone}=req.body;if(!name||!email||!password)return res.status(400).json({success:false,message:'Name, email and password are required.'});if(password.length<6)return res.status(400).json({success:false,message:'Password must be at least 6 characters.'});if(await User.findOne({email:email.toLowerCase()}))return res.status(409).json({success:false,message:'An account with this email already exists.'});const u=await User.create({name,email:email.toLowerCase(),password:await bcrypt.hash(password,12),phone});res.status(201).json({success:true,message:'Registration successful.',token:token(u._id),user:{id:u._id,name:u.name,email:u.email,phone:u.phone,role:u.role}})}catch(e){res.status(500).json({success:false,message:e.message})}};exports.login=async(req,res)=>{try{const{email,password}=req.body,u=await User.findOne({email:email?.toLowerCase()});if(!u||!(await bcrypt.compare(password||'',u.password)))return res.status(401).json({success:false,message:'Invalid email or password.'});res.json({success:true,message:'Login successful.',token:token(u._id),user:{id:u._id,name:u.name,email:u.email,phone:u.phone,role:u.role}})}catch(e){res.status(500).json({success:false,message:e.message})}};
