const User=require('../models/User');exports.getProfile=async(req,res)=>res.json({success:true,user:await User.findById(req.user._id).select('-password')});exports.updateProfile=async(req,res)=>{try{const u=await User.findByIdAndUpdate(req.user._id,{name:req.body.name,phone:req.body.phone,avatar:req.body.avatar},{new:true,runValidators:true}).select('-password');res.json({success:true,message:'Profile updated successfully.',user:u})}catch(e){res.status(400).json({success:false,message:e.message})}};
